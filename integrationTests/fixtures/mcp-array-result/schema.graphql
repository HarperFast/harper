# @export generates the MCP CRUD verb tools, each advertising a derived object
# outputSchema — the contract the resources.js getter deliberately breaks.
type Listing @table @export {
	id: ID @primaryKey
	name: String
}
